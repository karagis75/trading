# Copy to linux/.env and change the password before any real use.
# Used only on the Linux VPS. Do not use this file on the Windows home PC.
# Do not bake secrets into the image or commit linux/.env.

POSTGRES_USER=trading_app
POSTGRES_PASSWORD=trading
POSTGRES_DB=trading_history
