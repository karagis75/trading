# Copy to .env and change the password before any real use.
# Compose substitutes these into TRADING_DATABASE_URL.
# Do not bake secrets into the image or commit .env.

POSTGRES_USER=trading_app
POSTGRES_PASSWORD=trading
POSTGRES_DB=trading_history
